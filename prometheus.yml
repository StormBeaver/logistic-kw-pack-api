global:
  scrape_interval:     10s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets:
        - host.docker.internal:9100 # for grpc-server
        - host.docker.internal:9101 # for retranslator
